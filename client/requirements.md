## Packages
framer-motion | Complex animations for game states and UI transitions
canvas-confetti | Celebration effect when levels are completed

## Notes
Game logic is implemented using pure React + HTML5 Canvas API.
Progress is stored in localStorage for persistence.
Leaderboard uses the provided backend API.
